	<div class="generic_tabs">
		<ul class="tabs">
			{{#each tabs}}
				<li{{#if active}} class="active"{{/if}}>{{#if url}}<a href="{{url}}">{{label}}</a>{{else}}<span>{{label}}</span>{{/if}}</li>
			{{/each}}
		</ul>
	</div>