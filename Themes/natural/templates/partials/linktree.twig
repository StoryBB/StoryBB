<nav aria-label="breadcrumb">
	<ol>
	{% for breadcrumb in page.getLinkTree %}
		<li>
		{% if breadcrumb.url %}
			<a href="{{ breadcrumb.url }}">{{ breadcrumb.name }}</a>
		{% else %}
			{{ breadcrumb.name }}
		{% endif %}
		</li>
	{% endfor %}
	</ol>
</nav>