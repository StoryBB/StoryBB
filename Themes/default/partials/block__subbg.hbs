	<div class="block block_{{blocktype}}" id="block_{{instance}}">
		<div class="sub_bar">
			<h3 class="subbg{{#if icon}} main_icons {{icon}}{{/if}}">
				{{title}}
			</h3>
		</div>
		<div class="inline">
			{{content}}
		</div>
	</div>